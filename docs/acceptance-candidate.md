# D06 / D10 候选版本验收

本轮 D03–D10 编码、约定测试和短期演练完成，达到供人工审核的候选交付状态；不正式发布、不自动合并、不升级日常环境。四个依赖 PR 为 [运行保障 #12](https://github.com/JavaWeh/ACCP/pull/12)、[协作体验 #13](https://github.com/JavaWeh/ACCP/pull/13)、[Git 来源 #14](https://github.com/JavaWeh/ACCP/pull/14)、[容量与候选交付 #15](https://github.com/JavaWeh/ACCP/pull/15)。

## 功能集验证

- 全量 Linux race 通过，控制面 382.571 秒；事件连续处理、超时后 NAK、延迟重投及工具执行权锁等待的定向 race 23.257 秒通过。
- Go 单元、vet、依赖完整性，Web 类型/构建/格式及浏览器回归通过；交接下载组件覆盖项目诊断与审计导出。
- 契约检查通过：79 个正例、32 个反例、469 处 Schema 编译；工具回归在补入安装包和资源约束校验后共 15 项通过。
- 最新代码提交 `1419827` 的远程文档/契约、控制面、Web、生产基础和候选构建五组 CI 全部通过。[生产验收](https://github.com/JavaWeh/ACCP/actions/runs/35198535600) 包含新安装、最小权限、真实 PKCE/任务/Git 来源、Worker 故障、密钥轮换及真实 token 过期重新认证；[候选构建](https://github.com/JavaWeh/ACCP/actions/runs/35198535639) 包含五平台原生启动。
- 依赖扫描包括 Go 可达调用和两个 npm 锁文件；npm 未发现漏洞，Go 无可达漏洞。模块清单中的 OpenPGP 提示不属于应用导入的可达调用，不将模块提示隐藏成“无任何漏洞信息”。完整历史秘密扫描通过，仅保留管理员手册所述精确 SQL 误报排除。

## 产物与安装实测

[首轮候选构建](https://github.com/JavaWeh/ACCP/actions/runs/35189639266) 完成五个平台原生 Bridge 启动，产物清单为 `v0.5.0-rc.1`，CI 合并提交 `7809195fbb1ec20d1195f8b453c0eb9e91b67b07`，`dirty=false`。下载的 ZIP 摘要为 `c9607ccc8fc863aa38ee8459aab927c47da586acffb748c058e6b39484bf2237`，内部 13 项校验和全部匹配。Go/Web/API/运维镜像 SBOM 分别包含 44/110/37/7405 个组件；API 与 Worker 使用同一镜像。

首包实际升级失败：安装包缺少 Compose 绑定的 `configs/nats.conf`，Docker 将缺失源创建成目录，导致 NATS 无法启动。因此**首包不可交付**，不能以 CI 或摘要通过覆盖部署失败。打包清单已补齐，构建时检查生产与验收 Compose 的全部静态文件挂载；缺文件、误挂目录、相对路径越界与覆盖文件路径解析均有回归。

使用修复后的本地包重新部署，008→009 前向迁移及最小权限通过，退出维护后 doctor 11 项通过；官方浏览器连接真实 Keycloak 和 TLS，任务编辑、归档/恢复、绑定查询分页通过（10.8 秒）。本地包标记 dirty，仅用于复现和修复验证；随后执行以下干净远程产物复验。

修复提交 `29bfc59` 的 [五组远程 CI](https://github.com/JavaWeh/ACCP/actions/runs/35193827795) 已全部通过，包含五平台原生 Bridge 与打包时静态挂载检查。[修复版候选产物](https://github.com/JavaWeh/ACCP/actions/runs/35193827795/artifacts/10485127303) 已完整下载，ZIP 摘要为 `c2e7f1b87b75199f0774296f011c418c12838621553a8d009d2e5a0831584147`，13 项内部校验和及 5 项静态文件挂载检查全部通过。产物版本 `v0.5.0-rc.1`，CI 合并提交 `22e335d6cf161f570a247ae65932a81c13281661`，`dirty=false`。

下载的 API/Worker 镜像为 `sha256:bd6be2bb8b4f9f8471bc8cb43d55513a523fc07c45ccc567fd86f712a0657e15`，运维镜像为 `sha256:c50925ed40e70c13fd38acdc33b2e2ec2671f00197bf290c4e8d18ac1f2927fe`。镜像和 Windows Bridge 实际启动版本一致。独立验收环境先备份并核验，进入维护、停止执行后，使用下载包重新部署；退出维护后 doctor 11/11 通过。官方浏览器在真实 TLS/Keycloak 下验证 PKCE 登录、未开通主体拒绝、access token/ID token 区分和重新认证，3 项通过（8.5 秒）。日常环境未升级。

运维镜像的实际诊断输出为 1238 字符，项目一致性审计导出为 650 条记录加一行元数据；跨项目导出拒绝且不创建文件。原始内容只留隔离验收目录，不作为公开报告附件。已核对 `29bfc59..1419827` 的 API、Worker、Bridge、运维、Web、生产配置与打包代码差异为空；后续只调整容量工具、配额和证据。上述已安装产物与最新代码的发布运行时一致，构建提交标识仍分别保留，不混写为同一产物。候选附件保留 30 天，正式交接时由管理员保存校验后的包和清单。

## 恢复与容量

10 万任务恢复、43 表摘要、旧 Session 拒绝、分权重建和空 JetStream 恢复见 [运行保障报告](acceptance-product-operations.md)。实测 RPO 113.488 秒，包含恢复中断和人工操作耗时的完整 RTO 1237.055 秒。

未落实 CPU 配额以及实际配额下并行负载的原始失败报告均保留在 [容量报告](capacity-validation.md)。停止旧容量容器后，单独完整运行 5+20+5 分钟，实际 8 CPU/16 GiB 门槛通过，容器退出码 0。稳定/突发读 p95 为 33.023/48.432 ms，写 p95 为 41.540/73.659 ms，事件 p95 为 252.333/379.884 ms，错误率为 0%/0.020%。最低活跃 Run 20、心跳错误 0、20 次工具调用与成功账本一致，重复 Run/副作用和异常授权响应均为 0。

## 交接与边界

整套变更相对 `main` 的最终影响检查标为 CRITICAL，涉及 142 个文件、99 条执行流程；最后的报告提交仅涉及文档和脱敏结果，无运行时流程变化。高风险边界包括维护/恢复默认隔离、最小权限与 Session 撤销、Owner 移交后的历史责任、事件重投去重与工具执行权锁。分别以恢复及角色演练、权限和历史集成、全量 Linux race、独立副作用回执验证，不能用容量通过替代这些正确性检查。

本轮约定的功能、权限、部署恢复、短期容量和候选产物演练无未关闭阻塞项。代码与证据通过独立 PR 交付，按 #12 → #13 → #14 → #15 由人类审核、逐包集成；未执行自动合并。最后的报告提交继续经过文档、契约、私有文件/历史及远程 CI 检查。

后续由人类确定审核和正式发布安排；本轮不执行连续稳定性观察、多端联合验收、正式发布、日常环境升级，也不将单次短测扩展为这些项目的通过结论。
