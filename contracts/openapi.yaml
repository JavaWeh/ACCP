openapi: 3.1.1
info:
  title: ACCP Collaboration API
  version: 0.4.0
  description: ACCP M1–M4 runtime API. Human ownership, delegated execution, verified artifacts, independent
    approvals, MCP Gateway and React console. The existing 0.2 Adapter handshake remains compatible. Real
    AI-client joint acceptance is deferred by the project owner; see docs/m3-m4-platform.md.
  license:
    name: Apache-2.0
    url: https://www.apache.org/licenses/LICENSE-2.0
  contact:
    name: ACCP maintainers
    url: https://github.com/JavaWeh/ACCP
servers:
  - url: https://accp.example/api/v1
    description: Reserved example host; not a deployment
tags:
  - name: Tasks
    description: Tasks domain interfaces; draft v0.1
  - name: Identity
    description: Identity domain interfaces; draft v0.1
  - name: Contexts
    description: Contexts domain interfaces; draft v0.1
  - name: Artifacts
    description: Artifacts domain interfaces; draft v0.1
  - name: Governance
    description: Governance domain interfaces; draft v0.1
  - name: Events
    description: Events domain interfaces; draft v0.1
  - name: Adapters
    description: Adapters domain interfaces; draft v0.1
security:
  - HumanBearer: []
  - AgentBearer: []
paths:
  /projects/{id}/repositories:
    get:
      operationId: listRepositories
      tags:
        - Tasks
      summary: List configured repositories
      description: Read project repository configuration; first provider planned is GitHub.
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque resource identifier; scope is verified server-side.
          schema:
            type: string
            minLength: 1
        - name: cursor
          in: query
          required: false
          description: Opaque continuation cursor.
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: Maximum page size.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: Successful operation. Human ownership and resource scopes are validated by the server.
          headers:
            ETag:
              description: Quoted resource version when returning a versioned entity.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/RepositoryPage
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-milestone: M1
  /projects/{id}/tasks:
    post:
      operationId: createTask
      tags:
        - Tasks
      summary: Create a human-owned task
      description: Creates DRAFT. Owner must be an active human member; repository and Context versions must belong
        to this project.
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque resource identifier; scope is verified server-side.
          schema:
            type: string
            minLength: 1
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "201":
          description: Successful operation. Human ownership and resource scopes are validated by the server.
          headers:
            ETag:
              description: Quoted resource version when returning a versioned entity.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Task
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/domain.schema.json#/$defs/CreateTask
      x-accp-milestone: M1
    get:
      operationId: listTasks
      tags:
        - Tasks
      summary: List project tasks
      description: Read authorized project tasks. M2 also allows a scoped Agent Session in the same project.
      x-accp-actors:
        - HUMAN
        - AGENT
      security:
        - HumanBearer: []
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque resource identifier; scope is verified server-side.
          schema:
            type: string
            minLength: 1
        - name: cursor
          in: query
          required: false
          description: Opaque continuation cursor.
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: Maximum page size.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: Successful operation. Human ownership and resource scopes are validated by the server.
          headers:
            ETag:
              description: Quoted resource version when returning a versioned entity.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/TaskPage
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-milestone: M1
      x-accp-required-scope: tasks:read
  /tasks/{id}:
    get:
      operationId: getTask
      tags:
        - Tasks
      summary: Read task state
      description: Authoritative state; events are notifications, not a replacement for this read. M2 also allows a
        scoped Agent Session in the same project.
      x-accp-actors:
        - HUMAN
        - AGENT
      security:
        - HumanBearer: []
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque resource identifier; scope is verified server-side.
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Successful operation. Human ownership and resource scopes are validated by the server.
          headers:
            ETag:
              description: Quoted resource version when returning a versioned entity.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Task
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-milestone: M1
      x-accp-required-scope: tasks:read
  /tasks/{id}/commands:
    post:
      operationId: commandTask
      tags:
        - Tasks
      summary: Submit, retry or cancel a task
      description: Human Owner or ADMIN submits, retries or cancels execution with the current Task version. RETRY
        creates no Run until the next authorized claim. Cancellation closes the active Run; DONE is immutable.
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque resource identifier; scope is verified server-side.
          schema:
            type: string
            minLength: 1
        - $ref: "#/components/parameters/IdempotencyKey"
        - $ref: "#/components/parameters/IfMatch"
      responses:
        "200":
          description: Successful operation. Human ownership and resource scopes are validated by the server.
          headers:
            ETag:
              description: Quoted resource version when returning a versioned entity.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Task
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/domain.schema.json#/$defs/TaskCommand
      x-accp-milestone: M1
  /tasks/{id}/dependencies:
    post:
      operationId: createDependency
      tags:
        - Tasks
      summary: create Dependency
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics.
      x-accp-milestone: M2
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Scoped resource identifier.
          schema: &a1
            type: string
            pattern: ^[a-zA-Z][a-zA-Z0-9_.:-]{1,127}$
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for the same logical operation; retries must preserve body, version and fence.
          schema:
            type: string
            minLength: 8
            maxLength: 128
        - name: If-Match
          in: header
          required: true
          description: Quoted current Task version. Read Task again after assignment or dependency changes.
          schema:
            type: string
      responses:
        "201":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/TaskDependency
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/domain.schema.json#/$defs/CreateDependency
    get:
      operationId: listTaskDependencies
      tags:
        - Tasks
      summary: list Task Dependencies
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics.
      x-accp-milestone: M2
      x-accp-actors:
        - HUMAN
        - AGENT
      security:
        - HumanBearer: []
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Scoped resource identifier.
          schema: *a1
        - name: cursor
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/m2.schema.json#/$defs/TaskDependencyPage
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-required-scope: tasks:read
  /tasks/{id}/assignments:
    post:
      operationId: assignTask
      tags:
        - Tasks
      summary: assign Task
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics. Owner or ADMIN only. Target an Agent or the
        general capability queue; other queues return UNSUPPORTED_QUEUE.
      x-accp-milestone: M2
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Scoped resource identifier.
          schema: *a1
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for the same logical operation; retries must preserve body, version and fence.
          schema:
            type: string
            minLength: 8
            maxLength: 128
        - name: If-Match
          in: header
          required: true
          description: Quoted current Task version. Read Task again after assignment or dependency changes.
          schema:
            type: string
      responses:
        "201":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Assignment
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/domain.schema.json#/$defs/CreateAssignment
    get:
      operationId: listTaskAssignments
      tags:
        - Tasks
      summary: list Task Assignments
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics.
      x-accp-milestone: M2
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Scoped resource identifier.
          schema: *a1
        - name: cursor
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/m2.schema.json#/$defs/AssignmentPage
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
  /agents:
    post:
      operationId: registerAgent
      tags:
        - Identity
      summary: register Agent
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics.
      x-accp-milestone: M2
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for the same logical operation; retries must preserve body, version and fence.
          schema:
            type: string
            minLength: 8
            maxLength: 128
      responses:
        "201":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/AgentIdentity
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/m2.schema.json#/$defs/AgentRegistration
  /agent-sessions:
    post:
      operationId: createAgentSession
      tags:
        - Identity
      summary: create Agent Session
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics.
      x-accp-milestone: M2
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for the same logical operation; retries must preserve body, version and fence.
          schema:
            type: string
            minLength: 8
            maxLength: 128
      responses:
        "201":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/SessionGrant
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/domain.schema.json#/$defs/CreateSession
  /agent-sessions/{id}/revoke:
    post:
      operationId: revokeAgentSession
      tags:
        - Identity
      summary: revoke Agent Session
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics.
      x-accp-milestone: M2
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Scoped resource identifier.
          schema: *a1
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for the same logical operation; retries must preserve body, version and fence.
          schema:
            type: string
            minLength: 8
            maxLength: 128
        - name: If-Match
          in: header
          required: true
          description: Quoted current Session version.
          schema:
            type: string
      responses:
        "200":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/AgentSession
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/domain.schema.json#/$defs/Reason
  /adapters/handshake:
    post:
      operationId: handshakeAdapter
      tags:
        - Adapters
      summary: handshake Adapter
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics. Only protocol 0.2 is selected. M2 does not
        advertise mcp_uri because the Gateway is not implemented. The submitted manifest must match human
        registration.
      x-accp-milestone: M2
      x-accp-actors:
        - AGENT
      security:
        - AgentBearer: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for the same logical operation; retries must preserve body, version and fence.
          schema:
            type: string
            minLength: 8
            maxLength: 128
      responses:
        "200":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/m2.schema.json#/$defs/HandshakeResponse
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-required-scope: handshake
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/m2.schema.json#/$defs/HandshakeRequest
  /task-runs/claim:
    post:
      operationId: claimTaskRun
      tags:
        - Tasks
      summary: claim Task Run
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics.
      x-accp-milestone: M2
      x-accp-actors:
        - AGENT
      security:
        - AgentBearer: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for the same logical operation; retries must preserve body, version and fence.
          schema:
            type: string
            minLength: 8
            maxLength: 128
      responses:
        "201":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/ClaimResponse
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-required-scope: runs:claim
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/m2.schema.json#/$defs/ClaimRequest
  /task-runs/{id}:
    get:
      operationId: getTaskRun
      tags:
        - Tasks
      summary: get Task Run
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics.
      x-accp-milestone: M2
      x-accp-actors:
        - HUMAN
        - AGENT
      security:
        - HumanBearer: []
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Scoped resource identifier.
          schema: *a1
      responses:
        "200":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/TaskRun
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-required-scope: tasks:read
  /task-runs/{id}/heartbeat:
    post:
      operationId: heartbeatTaskRun
      tags:
        - Tasks
      summary: heartbeat Task Run
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics.
      x-accp-milestone: M2
      x-accp-actors:
        - AGENT
      security:
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Scoped resource identifier.
          schema: *a1
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for the same logical operation; retries must preserve body, version and fence.
          schema:
            type: string
            minLength: 8
            maxLength: 128
        - name: If-Match
          in: header
          required: true
          description: Quoted current Run version.
          schema:
            type: string
        - $ref: "#/components/parameters/Fencing"
      responses:
        "200":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/HeartbeatResponse
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-required-scope: runs:write
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/domain.schema.json#/$defs/HeartbeatRequest
  /task-runs/{id}/reports:
    post:
      operationId: reportTaskRun
      tags:
        - Tasks
      summary: report Task Run
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics. Completion candidates require verified stored
        artifacts and enter IN_REVIEW. External references remain UNVERIFIED. Agents cannot set Task DONE.
      x-accp-milestone: M2
      x-accp-actors:
        - AGENT
      security:
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Scoped resource identifier.
          schema: *a1
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for the same logical operation; retries must preserve body, version and fence.
          schema:
            type: string
            minLength: 8
            maxLength: 128
        - name: If-Match
          in: header
          required: true
          description: Quoted current Run version.
          schema:
            type: string
        - $ref: "#/components/parameters/Fencing"
      responses:
        "200":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/TaskRun
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-required-scope: runs:write
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/domain.schema.json#/$defs/RunReport
    get:
      operationId: listRunReports
      tags:
        - Tasks
      summary: List persisted execution reports
      description: Project humans and the owning Agent Session may inspect immutable progress, failure and
        completion-candidate reports. Sort by created_at for a timeline.
      x-accp-milestone: M2
      x-accp-actors:
        - HUMAN
        - AGENT
      security:
        - HumanBearer: []
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Scoped resource identifier.
          schema:
            type: string
            pattern: ^[a-zA-Z][a-zA-Z0-9_.:-]{1,127}$
        - name: cursor
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/m2.schema.json#/$defs/RunReportPage
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-required-scope: tasks:read
  /projects/{id}/contexts:
    post:
      operationId: createContext
      tags:
        - Contexts
      summary: Declare a Context source
      description: Human project member declares exactly one canonical source. M1 accepts only ACCP sources (422
        otherwise); canonical_uri is a label, never fetched.
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque resource identifier; scope is verified server-side.
          schema:
            type: string
            minLength: 1
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "201":
          description: Successful operation. Human ownership and resource scopes are validated by the server.
          headers:
            ETag:
              description: Quoted resource version when returning a versioned entity.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Context
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/domain.schema.json#/$defs/CreateContext
      x-accp-milestone: M1
    get:
      operationId: listContexts
      tags:
        - Contexts
      summary: List readable Context resources
      description: Current resource ACLs are checked before listing metadata. M2 also allows a scoped Agent Session
        in the same project.
      x-accp-actors:
        - HUMAN
        - AGENT
      security:
        - HumanBearer: []
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque resource identifier; scope is verified server-side.
          schema:
            type: string
            minLength: 1
        - name: cursor
          in: query
          required: false
          description: Opaque continuation cursor.
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: Maximum page size.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: Successful operation. Human ownership and resource scopes are validated by the server.
          headers:
            ETag:
              description: Quoted resource version when returning a versioned entity.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/ContextPage
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-milestone: M1
      x-accp-required-scope: context:read
  /contexts/{id}/versions:
    post:
      operationId: createContextVersion
      tags:
        - Contexts
      summary: Submit immutable candidate content
      description: "Verify allowed content URI and digest. New version is CANDIDATE even when submitted by an Agent.
        M1 requires a same-project urn:accp:content: URI from uploadContextContent; digest and media type must
        match stored bytes. Creation increments the parent Context version."
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque resource identifier; scope is verified server-side.
          schema:
            type: string
            minLength: 1
        - $ref: "#/components/parameters/IdempotencyKey"
        - $ref: "#/components/parameters/IfMatch"
      responses:
        "201":
          description: Successful operation. Human ownership and resource scopes are validated by the server.
          headers:
            ETag:
              description: Quoted resource version when returning a versioned entity.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/ContextVersion
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/domain.schema.json#/$defs/CreateContextVersion
      x-accp-milestone: M1
    get:
      operationId: listContextVersions
      tags:
        - Contexts
      summary: List immutable Context versions
      description: List immutable Context versions M2 also allows a scoped Agent Session in the same project.
      x-accp-milestone: M1
      x-accp-actors:
        - HUMAN
        - AGENT
      security:
        - HumanBearer: []
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque resource ID; scope is verified by the server.
          schema:
            type: string
            minLength: 1
        - &a4
          name: cursor
          in: query
          description: Opaque continuation bound to this collection.
          schema:
            type: string
        - &a5
          name: limit
          in: query
          description: Maximum page size.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: Successful M1 operation.
          headers:
            ETag:
              description: Quoted version for versioned resources.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/m1.schema.json#/$defs/ContextVersionPage
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-required-scope: context:read
  /contexts/{id}/versions/{version}:
    get:
      operationId: getContextVersion
      tags:
        - Contexts
      summary: Read a precise Context version
      description: Version path is a ContextVersion ID, not latest. Current ACL applies even to historical content.
        M2 also allows a scoped Agent Session in the same project.
      x-accp-actors:
        - HUMAN
        - AGENT
      security:
        - HumanBearer: []
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque resource identifier; scope is verified server-side.
          schema:
            type: string
            minLength: 1
        - name: version
          in: path
          required: true
          description: Opaque resource identifier; scope is verified server-side.
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Successful operation. Human ownership and resource scopes are validated by the server.
          headers:
            ETag:
              description: Quoted resource version when returning a versioned entity.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/ContextVersion
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-milestone: M1
      x-accp-required-scope: context:read
  /contexts/{id}/versions/{version}/publish:
    post:
      operationId: publishContextVersion
      tags:
        - Contexts
      summary: Publish reviewed Context content
      description: Human REVIEWER or ADMIN; verify source revision and content digest before publication. If-Match
        refers to the ContextVersion resource.
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque resource identifier; scope is verified server-side.
          schema:
            type: string
            minLength: 1
        - name: version
          in: path
          required: true
          description: Opaque resource identifier; scope is verified server-side.
          schema:
            type: string
            minLength: 1
        - $ref: "#/components/parameters/IdempotencyKey"
        - $ref: "#/components/parameters/IfMatch"
      responses:
        "200":
          description: Successful operation. Human ownership and resource scopes are validated by the server.
          headers:
            ETag:
              description: Quoted resource version when returning a versioned entity.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/ContextVersion
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/domain.schema.json#/$defs/PublishContext
      x-accp-milestone: M1
  /context-snapshots/{id}:
    get:
      operationId: getContextSnapshot
      tags:
        - Contexts
      summary: get Context Snapshot
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics.
      x-accp-milestone: M2
      x-accp-actors:
        - HUMAN
        - AGENT
      security:
        - HumanBearer: []
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Scoped resource identifier.
          schema: *a1
      responses:
        "200":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/ContextSnapshot
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-required-scope: context:read
  /task-runs/{id}/artifacts:
    post:
      operationId: registerArtifact
      tags:
        - Artifacts
      summary: register Artifact
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics.
      x-accp-milestone: M2
      x-accp-actors:
        - AGENT
      security:
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Scoped resource identifier.
          schema: *a1
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for the same logical operation; retries must preserve body, version and fence.
          schema:
            type: string
            minLength: 8
            maxLength: 128
        - name: If-Match
          in: header
          required: true
          description: Quoted current Run version.
          schema:
            type: string
        - $ref: "#/components/parameters/Fencing"
      responses:
        "201":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Artifact
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-required-scope: artifacts:write
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/domain.schema.json#/$defs/RegisterArtifact
    get:
      operationId: m3ListRunartifacts
      tags:
        - Artifacts
      summary: List Run artifacts
      description: List Run artifacts. Authorization, project boundary, idempotency and resource versions are
        checked by the server.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
        - AGENT
      security:
        - HumanBearer: []
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: &a2
            type: string
            minLength: 1
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema: &a3
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
                  next_cursor:
                    type:
                      - string
                      - "null"
                required:
                  - items
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
  /artifacts/{id}:
    get:
      operationId: getArtifact
      tags:
        - Artifacts
      summary: get Artifact
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics.
      x-accp-milestone: M2
      x-accp-actors:
        - HUMAN
        - AGENT
      security:
        - HumanBearer: []
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Scoped resource identifier.
          schema: *a1
      responses:
        "200":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Artifact
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-required-scope: tasks:read
  /artifacts/{id}/reviews:
    post:
      operationId: m3Reviewartifactevidence
      tags:
        - Artifacts
      summary: Review artifact evidence
      description: Review artifact evidence. Authorization, project boundary, idempotency and resource versions are
        checked by the server.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for this logical write; reuse on uncertain retries.
          schema:
            type: string
            minLength: 8
            maxLength: 128
        - name: If-Match
          in: header
          required: true
          description: Quoted current resource version.
          schema:
            type: string
            pattern: ^"[1-9][0-9]*"$
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Artifact
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/m3.schema.json#/$defs/ArtifactReview
    get:
      operationId: m3Listartifactreviews
      tags:
        - Artifacts
      summary: List artifact reviews
      description: List artifact reviews. Authorization, project boundary, idempotency and resource versions are
        checked by the server.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema: *a3
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
  /approvals:
    get:
      operationId: listApprovals
      tags:
        - Governance
      summary: List project approvals
      description: Current project reviewer permissions control visible details.
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: project_id
          in: query
          required: true
          description: Project must be authorized by current identity.
          schema:
            type: string
            minLength: 1
        - name: cursor
          in: query
          required: false
          description: Opaque continuation cursor.
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: Maximum page size.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: Successful operation. Human ownership and resource scopes are validated by the server.
          headers:
            ETag:
              description: Quoted resource version when returning a versioned entity.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/ApprovalPage
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-milestone: future
  /approvals/{id}:
    get:
      operationId: m3Readapprovalbinding
      tags:
        - Governance
      summary: Read approval binding
      description: Read approval binding. Authorization, project boundary, idempotency and resource versions are
        checked by the server.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema:
                $ref: ./schemas/m3.schema.json#/$defs/Approval
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
  /approvals/{id}/decisions:
    post:
      operationId: m3Decideexactoperationapproval
      tags:
        - Governance
      summary: Decide exact operation approval
      description: Decide exact operation approval. Authorization, project boundary, idempotency and resource
        versions are checked by the server.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for this logical write; reuse on uncertain retries.
          schema:
            type: string
            minLength: 8
            maxLength: 128
        - name: If-Match
          in: header
          required: true
          description: Quoted current resource version.
          schema:
            type: string
            pattern: ^"[1-9][0-9]*"$
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema:
                $ref: ./schemas/m3.schema.json#/$defs/Approval
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/m3.schema.json#/$defs/ApprovalDecision
  /tool-invocations/{id}:
    get:
      operationId: m3Readtooloperationresult
      tags:
        - Governance
      summary: Read tool operation result
      description: Read tool operation result. Authorization, project boundary, idempotency and resource versions
        are checked by the server.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
        - AGENT
      security:
        - HumanBearer: []
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema:
                $ref: ./schemas/m3.schema.json#/$defs/Invocation
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
  /tasks/{id}/reviews:
    post:
      operationId: m3Owneracceptsorreturnstask
      tags:
        - Tasks
      summary: Owner accepts or returns task
      description: Owner accepts or returns task. Authorization, project boundary, idempotency and resource versions
        are checked by the server.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for this logical write; reuse on uncertain retries.
          schema:
            type: string
            minLength: 8
            maxLength: 128
        - name: If-Match
          in: header
          required: true
          description: Quoted current resource version.
          schema:
            type: string
            pattern: ^"[1-9][0-9]*"$
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Task
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/m3.schema.json#/$defs/TaskReview
    get:
      operationId: m3Listimmutabletaskreviews
      tags:
        - Tasks
      summary: List immutable task reviews
      description: List immutable task reviews. Authorization, project boundary, idempotency and resource versions
        are checked by the server.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema: *a3
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
  /events:
    get:
      operationId: listEvents
      tags:
        - Events
      summary: list Events
      description: "Implemented in M2. Authorization is derived from current identity, membership and delegation.
        See docs/m2-execution.md for state and recovery semantics. Accept: text/event-stream returns bounded
        20-second SSE streams. Each accp.events message carries a complete EventPage and its cursor. Reconnect
        using Last-Event-ID. Every page rechecks authorization; seven-day cursor retention. The accp.closed
        event requests reauthentication or reconciliation."
      x-accp-milestone: M2
      x-accp-actors:
        - HUMAN
        - AGENT
      security:
        - HumanBearer: []
        - AgentBearer: []
      parameters:
        - name: project_id
          in: query
          required: true
          schema: *a1
        - name: cursor
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: Last-Event-ID
          in: header
          description: SSE page cursor; query cursor takes precedence.
          schema:
            type: string
      responses:
        "200":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/m2.schema.json#/$defs/EventPage
            text/event-stream:
              schema:
                type: string
                description: SSE accp.events page frames, followed optionally by accp.closed.
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-required-scope: events:read
  /audit-records:
    get:
      operationId: listAuditRecords
      tags:
        - Governance
      summary: Read responsibility and evidence trail
      description: Humans with project audit access. Raw credentials and full sensitive Context bodies are never returned.
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: project_id
          in: query
          required: true
          description: Project must be authorized by current identity.
          schema:
            type: string
            minLength: 1
        - name: cursor
          in: query
          required: false
          description: Opaque continuation cursor.
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: Maximum page size.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: Successful operation. Human ownership and resource scopes are validated by the server.
          headers:
            ETag:
              description: Quoted resource version when returning a versioned entity.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/AuditPage
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-milestone: future
  /me:
    get:
      operationId: getCurrentHuman
      tags:
        - Identity
      summary: Read the provisioned human identity
      description: Read the provisioned human identity
      x-accp-milestone: M1
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters: []
      responses:
        "200":
          description: Successful M1 operation.
          headers:
            ETag:
              description: Quoted version for versioned resources.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/m1.schema.json#/$defs/Human
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
  /projects:
    get:
      operationId: listProjects
      tags:
        - Identity
      summary: List projects with active membership
      description: List projects with active membership
      x-accp-milestone: M1
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - *a4
        - *a5
      responses:
        "200":
          description: Successful M1 operation.
          headers:
            ETag:
              description: Quoted version for versioned resources.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/m1.schema.json#/$defs/ProjectPage
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
  /projects/{id}/members:
    get:
      operationId: listProjectMembers
      tags:
        - Identity
      summary: List provisioned project members
      description: List provisioned project members
      x-accp-milestone: M1
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque resource ID; scope is verified by the server.
          schema:
            type: string
            minLength: 1
        - *a4
        - *a5
      responses:
        "200":
          description: Successful M1 operation.
          headers:
            ETag:
              description: Quoted version for versioned resources.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/m1.schema.json#/$defs/MembershipPage
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
  /projects/{id}/members/{user}/changes:
    post:
      operationId: changeProjectMember
      tags:
        - Identity
      summary: Administrator changes member roles or active status
      description: Requires ADMIN; cannot remove the last active human administrator. The member version is checked,
        and permission revocation applies to subsequent requests including idempotent replays.
      x-accp-milestone: M1
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque resource ID; scope is verified by the server.
          schema:
            type: string
            minLength: 1
        - name: user
          in: path
          required: true
          description: Opaque resource ID; scope is verified by the server.
          schema:
            type: string
            minLength: 1
        - $ref: "#/components/parameters/IdempotencyKey"
        - $ref: "#/components/parameters/IfMatch"
      responses:
        "200":
          description: Successful M1 operation.
          headers:
            ETag:
              description: Quoted version for versioned resources.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/m1.schema.json#/$defs/Membership
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/m1.schema.json#/$defs/MembershipChange
  /projects/{id}/contents:
    post:
      operationId: uploadContextContent
      tags:
        - Contexts
      summary: Upload immutable text content
      description: MEMBER or ADMIN uploads UTF-8 text (at most 256 KiB, no NUL). Server computes SHA-256; bytes live
        in PostgreSQL in M1. Does not publish a Context version.
      x-accp-milestone: M1
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque resource ID; scope is verified by the server.
          schema:
            type: string
            minLength: 1
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "201":
          description: Successful M1 operation.
          headers:
            ETag:
              description: Quoted version for versioned resources.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/m1.schema.json#/$defs/ContentMetadata
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/m1.schema.json#/$defs/UploadContent
  /contents/{id}:
    get:
      operationId: getContextContent
      tags:
        - Contexts
      summary: Read immutable text with current project authorization
      description: Read immutable text with current project authorization M2 also allows a scoped Agent Session in
        the same project.
      x-accp-milestone: M1
      x-accp-actors:
        - HUMAN
        - AGENT
      security:
        - HumanBearer: []
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque resource ID; scope is verified by the server.
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Successful M1 operation.
          headers:
            ETag:
              description: Quoted version for versioned resources.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/m1.schema.json#/$defs/Content
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-required-scope: context:read
  /projects/{id}/audit-records:
    get:
      operationId: listProjectAuditRecords
      tags:
        - Governance
      summary: Read successful M1 mutation audit records
      description: REVIEWER or ADMIN. Only successful human mutations are persisted in M1; no Agent, TaskRun or
        approval evidence exists yet.
      x-accp-milestone: M1
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque resource ID; scope is verified by the server.
          schema:
            type: string
            minLength: 1
        - *a4
        - *a5
      responses:
        "200":
          description: Successful M1 operation.
          headers:
            ETag:
              description: Quoted version for versioned resources.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/AuditPage
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
  /contexts/{id}:
    get:
      operationId: getContext
      tags:
        - Contexts
      summary: Read Context metadata and its current version
      description: Read Context metadata and its current version M2 also allows a scoped Agent Session in the same
        project.
      x-accp-milestone: M1
      x-accp-actors:
        - HUMAN
        - AGENT
      security:
        - HumanBearer: []
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque resource ID; scope is verified by the server.
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Successful M1 operation.
          headers:
            ETag:
              description: Quoted version for versioned resources.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Context
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-required-scope: context:read
  /projects/{id}/agents:
    get:
      operationId: listAgents
      tags:
        - Identity
      summary: list Agents
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics.
      x-accp-milestone: M2
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Scoped resource identifier.
          schema: *a1
        - name: cursor
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/m2.schema.json#/$defs/AgentIdentityPage
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
  /agent-sessions/{id}:
    get:
      operationId: getAgentSession
      tags:
        - Identity
      summary: get Agent Session
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics.
      x-accp-milestone: M2
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Scoped resource identifier.
          schema: *a1
      responses:
        "200":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/AgentSession
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
  /tasks/{id}/dependencies/{dependency}/remove:
    post:
      operationId: removeTaskDependency
      tags:
        - Tasks
      summary: remove Task Dependency
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics.
      x-accp-milestone: M2
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Scoped resource identifier.
          schema: *a1
        - name: dependency
          in: path
          required: true
          description: Scoped resource identifier.
          schema: *a1
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for the same logical operation; retries must preserve body, version and fence.
          schema:
            type: string
            minLength: 8
            maxLength: 128
        - name: If-Match
          in: header
          required: true
          description: Quoted current Task version. Read Task again after assignment or dependency changes.
          schema:
            type: string
      responses:
        "200":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Task
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/domain.schema.json#/$defs/Reason
  /tasks/{id}/runs:
    get:
      operationId: listTaskRuns
      tags:
        - Tasks
      summary: list Task Runs
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics.
      x-accp-milestone: M2
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Scoped resource identifier.
          schema: *a1
        - name: cursor
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/m2.schema.json#/$defs/TaskRunPage
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
  /task-runs/{id}/artifact-contents:
    post:
      operationId: uploadArtifactContent
      tags:
        - Artifacts
      summary: upload Artifact Content
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics.
      x-accp-milestone: M2
      x-accp-actors:
        - AGENT
      security:
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Scoped resource identifier.
          schema: *a1
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for the same logical operation; retries must preserve body, version and fence.
          schema:
            type: string
            minLength: 8
            maxLength: 128
        - name: If-Match
          in: header
          required: true
          description: Quoted current Run version.
          schema:
            type: string
        - $ref: "#/components/parameters/Fencing"
      responses:
        "201":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/m2.schema.json#/$defs/ArtifactContent
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-required-scope: artifacts:write
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/m1.schema.json#/$defs/UploadContent
  /artifact-contents/{id}:
    get:
      operationId: getArtifactContent
      tags:
        - Artifacts
      summary: get Artifact Content
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics.
      x-accp-milestone: M2
      x-accp-actors:
        - HUMAN
        - AGENT
      security:
        - HumanBearer: []
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Scoped resource identifier.
          schema: *a1
      responses:
        "200":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/m2.schema.json#/$defs/ArtifactContentBody
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      x-accp-required-scope: tasks:read
  /projects/{id}/events/{event}/replay:
    post:
      operationId: replayEvent
      tags:
        - Events
      summary: replay Event
      description: Implemented in M2. Authorization is derived from current identity, membership and delegation. See
        docs/m2-execution.md for state and recovery semantics. ADMIN only. Requeues the original event ID;
        already processed Inbox entries do not create another feed entry or external side effect.
      x-accp-milestone: M2
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Scoped resource identifier.
          schema: *a1
        - name: event
          in: path
          required: true
          description: Scoped resource identifier.
          schema: *a1
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for the same logical operation; retries must preserve body, version and fence.
          schema:
            type: string
            minLength: 8
            maxLength: 128
      responses:
        "202":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: ./schemas/m2.schema.json#/$defs/ReplayReceipt
        "400":
          $ref: "#/components/responses/Problem"
        "401":
          $ref: "#/components/responses/Problem"
        "403":
          $ref: "#/components/responses/Problem"
        "404":
          $ref: "#/components/responses/Problem"
        "409":
          $ref: "#/components/responses/Problem"
        "410":
          $ref: "#/components/responses/Problem"
        "412":
          $ref: "#/components/responses/Problem"
        "422":
          $ref: "#/components/responses/Problem"
        "428":
          $ref: "#/components/responses/Problem"
        "503":
          $ref: "#/components/responses/Problem"
        default:
          $ref: "#/components/responses/Problem"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/domain.schema.json#/$defs/Reason
  /projects/{id}/artifacts:
    get:
      operationId: m3Listprojectartifacts
      tags:
        - Artifacts
      summary: List project artifacts
      description: List project artifacts. Authorization, project boundary, idempotency and resource versions are
        checked by the server.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema: *a3
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
  /artifacts/{id}/verifications:
    post:
      operationId: m3VerifyimmutableGitevidence
      tags:
        - Artifacts
      summary: Verify immutable Git evidence
      description: Verify immutable Git evidence. Authorization, project boundary, idempotency and resource versions
        are checked by the server.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for this logical write; reuse on uncertain retries.
          schema:
            type: string
            minLength: 8
            maxLength: 128
        - name: If-Match
          in: header
          required: true
          description: Quoted current resource version.
          schema:
            type: string
            pattern: ^"[1-9][0-9]*"$
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Artifact
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/m3.schema.json#/$defs/Reason
    get:
      operationId: m3Listartifactverifications
      tags:
        - Artifacts
      summary: List artifact verifications
      description: List artifact verifications. Authorization, project boundary, idempotency and resource versions
        are checked by the server.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema: *a3
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
  /projects/{id}/tool-backends:
    get:
      operationId: m3Listoperatorconfiguredbackends
      tags:
        - Governance
      summary: List operator configured backends
      description: List operator configured backends. Authorization, project boundary, idempotency and resource
        versions are checked by the server.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema: *a3
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
  /projects/{id}/tools:
    get:
      operationId: m3Listprojecttools
      tags:
        - Governance
      summary: List project tools
      description: List project tools. Authorization, project boundary, idempotency and resource versions are
        checked by the server.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
        - AGENT
      security:
        - HumanBearer: []
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema:
                $ref: ./schemas/m3.schema.json#/$defs/PolicyPage
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
    post:
      operationId: m3Registerprojecttoolpolicy
      tags:
        - Governance
      summary: Register project tool policy
      description: Register project tool policy. Authorization, project boundary, idempotency and resource versions
        are checked by the server.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for this logical write; reuse on uncertain retries.
          schema:
            type: string
            minLength: 8
            maxLength: 128
      responses:
        "201":
          description: Operation succeeded.
          content:
            application/json:
              schema:
                $ref: ./schemas/m3.schema.json#/$defs/Policy
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/m3.schema.json#/$defs/PolicyRequest
  /tools/{id}/changes:
    post:
      operationId: m3Changetoolpolicy
      tags:
        - Governance
      summary: Change tool policy
      description: Change tool policy. Authorization, project boundary, idempotency and resource versions are
        checked by the server.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for this logical write; reuse on uncertain retries.
          schema:
            type: string
            minLength: 8
            maxLength: 128
        - name: If-Match
          in: header
          required: true
          description: Quoted current resource version.
          schema:
            type: string
            pattern: ^"[1-9][0-9]*"$
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema:
                $ref: ./schemas/m3.schema.json#/$defs/Policy
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/m3.schema.json#/$defs/PolicyRequest
  /task-runs/{id}/tool-invocations:
    post:
      operationId: m3Requestgovernedtooloperation
      tags:
        - Governance
      summary: Request governed tool operation
      description: Persist an immutable operation. HIGH-risk operations wait for an independent human approval;
        Worker revalidates the live Session, Run, resource and policy immediately before execution. UNKNOWN
        results are never retried automatically.
      x-accp-milestone: M3
      x-accp-actors:
        - AGENT
      security:
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for this logical write; reuse on uncertain retries.
          schema:
            type: string
            minLength: 8
            maxLength: 128
        - name: If-Match
          in: header
          required: true
          description: Quoted current resource version.
          schema:
            type: string
            pattern: ^"[1-9][0-9]*"$
        - name: X-Run-Fencing-Token
          in: header
          required: true
          description: Current Run fencing token.
          schema:
            type: integer
            minimum: 1
      responses:
        "201":
          description: Operation succeeded.
          content:
            application/json:
              schema:
                $ref: ./schemas/m3.schema.json#/$defs/Invocation
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/m3.schema.json#/$defs/InvocationRequest
  /projects/{id}/tool-invocations:
    get:
      operationId: m3Listprojecttooloperations
      tags:
        - Governance
      summary: List project tool operations
      description: List project tool operations. Authorization, project boundary, idempotency and resource versions
        are checked by the server.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema:
                $ref: ./schemas/m3.schema.json#/$defs/InvocationPage
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
  /tool-invocations/{id}/reconcile:
    post:
      operationId: m3Reconcileunknownexternaloutcome
      tags:
        - Governance
      summary: Reconcile unknown external outcome
      description: Reconcile unknown external outcome. Authorization, project boundary, idempotency and resource
        versions are checked by the server.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
        - name: Idempotency-Key
          in: header
          required: true
          description: Stable key for this logical write; reuse on uncertain retries.
          schema:
            type: string
            minLength: 8
            maxLength: 128
        - name: If-Match
          in: header
          required: true
          description: Quoted current resource version.
          schema:
            type: string
            pattern: ^"[1-9][0-9]*"$
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema:
                $ref: ./schemas/m3.schema.json#/$defs/Invocation
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ./schemas/m3.schema.json#/$defs/Reason
  /projects/{id}/approvals:
    get:
      operationId: m3Listprojectapprovals
      tags:
        - Governance
      summary: List project approvals
      description: List project approvals. Authorization, project boundary, idempotency and resource versions are
        checked by the server.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema:
                $ref: ./schemas/m3.schema.json#/$defs/ApprovalPage
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
  /agent-sessions/{id}/gateway-token:
    get:
      operationId: m3ReadresourcescopedGatewaycredential
      tags:
        - Identity
      summary: Read resource scoped Gateway credential
      description: Only the current delegator or same Session with tools:invoke may obtain this credential. The
        token audience is the configured /mcp resource. It is never a downstream credential or persisted
        response.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
        - AGENT
      security:
        - HumanBearer: []
        - AgentBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema:
                $ref: ./schemas/m3.schema.json#/$defs/GatewayGrant
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
  /projects/{id}/agent-sessions:
    get:
      operationId: m3ListprojectAgentSessions
      tags:
        - Identity
      summary: List project Agent Sessions
      description: List project Agent Sessions. Authorization, project boundary, idempotency and resource versions
        are checked by the server.
      x-accp-milestone: M3
      x-accp-actors:
        - HUMAN
      security:
        - HumanBearer: []
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque authorized resource ID.
          schema: *a2
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema: *a3
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
  /auth/config:
    get:
      operationId: m3ReadpublicConsoleauthenticationconfiguration
      tags:
        - Identity
      summary: Read public Console authentication configuration
      description: Read public Console authentication configuration. Authorization, project boundary, idempotency
        and resource versions are checked by the server.
      x-accp-milestone: M4
      x-accp-actors:
        - HUMAN
      security: []
      parameters: []
      responses:
        "200":
          description: Operation succeeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  mode:
                    type: string
                  issuer:
                    type: string
                  client_id:
                    type: string
                  public_url:
                    type: string
                required:
                  - mode
                  - issuer
                  - client_id
                  - public_url
                additionalProperties: false
        "400":
          description: Invalid request or state.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "403":
          description: Current identity or scope is not authorized.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "409":
          description: State, approval binding or idempotency conflict.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
        "412":
          description: Resource version or evidence changed.
          content:
            application/problem+json:
              schema:
                $ref: ./schemas/domain.schema.json#/$defs/Problem
components:
  securitySchemes:
    HumanBearer:
      type: http
      scheme: bearer
      bearerFormat: Human access token
      description: Validated OIDC-backed human identity for ACCP; project roles checked server-side.
    AgentBearer:
      type: http
      scheme: bearer
      bearerFormat: Scoped ACCP session token
      description: Short-lived audience-bound token; session id alone is not a credential.
  parameters:
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      description: Unique per actor, route and resource; same key with different input returns 409.
      schema:
        type: string
        minLength: 8
        maxLength: 128
    IfMatch:
      name: If-Match
      in: header
      required: true
      description: Quoted resource version returned as ETag; 412 on stale version, 428 when absent.
      schema:
        type: string
        pattern: ^"[1-9][0-9]*"$
    Fencing:
      name: X-Run-Fencing-Token
      in: header
      required: true
      description: Current server-issued run fencing token; stale or expired leases cannot write.
      schema:
        type: integer
        minimum: 1
  responses:
    Problem:
      description: Authentication, authorization, version or domain failure; see code and trace_id.
      content:
        application/problem+json:
          schema:
            $ref: ./schemas/domain.schema.json#/$defs/Problem
